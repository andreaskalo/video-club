// document.addEventListener("click", (event) => {
//   const clickedCard = event.target.closest(".card_container");

//   if (!clickedCard) {
//     return;
//   }

//   const itemId = clickedCard.dataset.itemId;
//   const isMovie = clickedCard.dataset.isMovie;

//   if(isMovie){
//     window.
//   }
// });